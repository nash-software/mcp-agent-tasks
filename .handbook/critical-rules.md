# Critical Handbook Rules

<!-- Auto-generated by `handbook update`. Do not edit manually. -->

## [HIGH] Guard conditions protect against invalid state transitions and operations

## [HIGH] Idempotency guards prevent duplicate state from repeated operations

## [HIGH] Circular dependency detection in task dependencies

## [HIGH] Maximum nesting depth of 3 levels prevents excessive subtask hierarchies

## [HIGH] Validation uses sentinel Sets (VALID_TYPES, VALID_PRIORITIES) for enum enforcement

## [HIGH] Action button endpoints

## [HIGH] build-task-summary script output and idempotency

## [HIGH] Concurrency: claim conflicts

## [HIGH] install command idempotency

## [HIGH] Task lifecycle: create → claim → link-commit → link-pr (merged) → done → archive

## [HIGH] Reconciler reads milestones.yaml

## [HIGH] rebuild-index --prune-orphans behavior

## [HIGH] Rebuild SQLite index from markdown files

## [HIGH] reference-sync: task_references ↔ SQLite

## [HIGH] serve-ui HTML dashboard endpoints

## [HIGH] serve-ui HTTP API endpoints

## [HIGH] Subtask promotion

## [HIGH] POST /api/transcribe

## [HIGH] Voice capture task creation endpoints

## [HIGH] resolveServerDbPath configuration lookup

## [HIGH] loadConfig() cwd-independence

## [HIGH] pickBestBinary platform-specific binary selection

## [HIGH] sanitizeContent redaction rules

## [HIGH] buildPrompt message and character budgeting

## [HIGH] extractIntents noise filtering rules

## [HIGH] normalizePath path normalization rules

## [HIGH] readConfig configuration file loading with fallback defaults

## [HIGH] routeProject CWD ancestor matching and specificity selection

## [HIGH] routeProject prefix hint matching fallback

## [HIGH] routeProject GEN fallback and lock contention handling

## [HIGH] stop-intent-extractor hook validation and silent fallback behavior

## [HIGH] Binary resolution with npm prefix and PATH fallback

## [HIGH] Path validation security guard against traversal

## [HIGH] Message character and count budgets for LLM prompts

## [HIGH] Regex patterns for redacting sensitive data from prompts

## [HIGH] Sanitize individual task fields (title, why, spec_file)

## [HIGH] Load and parse index.yaml task metadata file

## [HIGH] Auto-initialize GEN global project on first use

## [HIGH] Atomic config file write via temp-file rename

## [HIGH] Compute aggregated routing hint from config and state

## [HIGH] Resolve agent-tasks executable with npm prefix fallback

## [HIGH] stop-intent-extractor hook validation and silent fallback behavior

## [HIGH] task.schema.json — area + scheduled_for field definitions

## [HIGH] resolveArea — fallback precedence with project mapping

## [HIGH] MarkdownStore — area + scheduled_for frontmatter persistence

## [HIGH] SqliteIndex — area + scheduled_for column storage and migration

## [HIGH] TaskFactory — scheduled_for null initialization and area resolution

## [HIGH] POST /api/capture/quick — input validation and response

## [HIGH] Quick capture — #PREFIX pattern extraction for routing

## [HIGH] Quick capture — LLM spawn fallback and prompt building

## [HIGH] CaptureOverlay — React component and hook structure

## [HIGH] SqliteIndex.getTasksByScheduledDate — date-filtered task retrieval

## [HIGH] SqliteIndex.getCandidates — unscheduled task filtering

## [HIGH] Capacity calculation — estimate_hours aggregation logic

## [HIGH] Target capacity parameter — bounds validation (60–600 minutes)

## [HIGH] Task schedule endpoint date validation

## [HIGH] TodayView component structure and rendering

## [HIGH] TodayView — capacity gauge conditional color logic

## [HIGH] useToday — hook implementation with polling and helpers

## [HIGH] api.ts — today endpoint client functions

## [HIGH] App.tsx — TodayView tab routing and default

## [HIGH] Header.tsx — today tab in navigation order

## [HIGH] POST /api/capture/braindump endpoint validation

## [HIGH] braindump JSON extraction logic with area field fallback

## [HIGH] POST /api/capture/commit endpoint task creation

## [HIGH] POST /api/acr/dispatch endpoint validation and offline handling

## [HIGH] GET /api/artifacts endpoint

## [HIGH] GET /api/artifacts data processing and sorting

## [HIGH] POST /api/artifacts/opened endpoint

## [HIGH] Artifact JSONL file persistence

## [HIGH] GET /api/acr/status — online path

## [HIGH] GET /api/acr/status — offline path

## [HIGH] GET /api/brain/search — online path

## [HIGH] GET /api/brain/search — offline path

## [HIGH] GET /api/brain/search — validation
